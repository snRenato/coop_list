<li id="item_<%= item.id %>" class="flex items-center justify-between p-3 border rounded-lg shadow hover:bg-gray-50 transition-colors duration-200
  <%= 'opacity-50 line-through' if item.status %>">
  
  <%= form_with model: [item.list, item], url: toggle_list_item_path(item.list, item), method: :patch, data: { turbo_frame: "items_list" }, class: "flex items-center gap-2 flex-1" do |f| %>
    <%= f.check_box :status, checked: item.status, onchange: "this.form.submit();", class: "w-5 h-5" %>
    <span class="flex-1"><%= item.name %></span>
  <% end %>

  <span class="text-sm font-semibold <%= item.status ? 'text-green-600' : 'text-gray-500' %>">
    <%= item.status ? "Concluído" : "Pendente" %>
  </span>
</li>